{% import "macros.j2" as macros with context %}

source s_local {
  system();
  internal();
};

{# Destination generation #}
{{ macros.generate_destinations(syslog_ng_client_destinations) }}

{% include "common/syslog-ng/" + syslog_ng_config_version + "/filters.j2" %}

{# Generate logpaths #}
{{ macros.generate_logpaths(syslog_ng_client_destinations, "destination") }}
